# ChampMail - Backend Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Graph Database (ChampGraph via HTTP)
# No additional dependency — uses httpx already listed below

# PostgreSQL
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Encryption
cryptography>=42.0.0

# Validation & Serialization
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# HTTP Client (for OpenRouter API calls)
httpx>=0.26.0

# Redis (caching + Celery broker)
redis>=5.0.0

# Async support
anyio>=4.2.0

# Environment
python-dotenv>=1.0.0

# Celery (Task Orchestration)
celery>=5.3.0
kombu>=5.3.0

# Cloudflare DNS Management
cloudflare>=4.0.0

# Email parsing
email-validator>=2.0.0

# Rate limiting
slowapi>=0.1.0

# Async file I/O
aiofiles>=23.0.0

# CSV processing
pandas>=2.0.0

# Timezone detection
pytz>=2024.1
timezonefinder>=6.0.0

# Thesys C1 (OpenAI-compatible SDK)
openai>=1.12.0

# CLI (CLI-Anything pattern)
click>=8.1.0
prompt-toolkit>=3.0.0

# Document parsing (Campaign Docs RAG)
python-docx>=1.1.0
